version: '3'

services:
    web:
        build: .
        ports:
            - 8000:8000
        # command: bash -c "cd ./s3cwebsite && pip install -r requirements.txt && python manage.py migrate && python manage.py runserver 0.0.0.0:8000"